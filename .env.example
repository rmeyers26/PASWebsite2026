# Optional NASA API key used at build time by ApodOfTheDay.astro, only as a
# fallback if the primary apod.com RSS feed can't be reached. Get a free key
# at https://api.nasa.gov; leave unset to fall back to the shared DEMO_KEY.
NASA_API_KEY=
